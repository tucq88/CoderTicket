
<%= link_to 'New Event', new_my_event_path, :class => 'btn btn-primary' %>

<div class="container-fluid events-list">
    <% @events.in_groups_of(3) do |g| %>
      <div class="row">
        <% g.compact.each do |e| %>
            <%= render partial: 'events/card', locals: {event: e} %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
